# Changesets

This folder is used by [Changesets](https://github.com/changesets/changesets) to track package version bumps.

To add a changeset:

```bash
pnpm changeset
```

To version packages (run during release):

```bash
pnpm version-packages
```
